def breakingRecords(scores):

    
    countmin=0
    countmax=0

    mini= scores[0]
    maxi= scores[0]
    for i in range(n):
        if scores[i]>maxi:
            maxi= scores[i]
            countmax=countmax+1
        if scores[i]<mini:
            mini=scores[i]
            countmin=countmin+1
    return countmax, countmin
